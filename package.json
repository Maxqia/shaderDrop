{
  "name": "shaderdrop-monorepo",
  "private": true,
  "scripts": {
    "test": "mocha -r ts-node/register --bail src/**/*.test.ts",
    "server": "ts-node src/server.ts"
  },
  "devDependencies": {
    "@types/node": "^12.12.17",
    "lerna": "^3.19.0",
    "typescript": "^3.7.3"
  }
}
