
npm install -g browserify watchify
npm install -g http-server

# all deps
npm install ws isomorphic-ws tweetnacl tweetnacl-util
# scanner deps
npm install instascan
# webclient deps
npm install jquery qrcode
# command deps
npm install readline qrcode-terminal


openssl req -newkey rsa:2048 -nodes -keyout key.pem -x509 -days 365 -out cert.pem

watchify scanner.js -o public/scanner.js
watchify xtermclient.js -o public/xtermclient.js

http-server --ssl --cert cert.pem --key key.pem
node server.js
